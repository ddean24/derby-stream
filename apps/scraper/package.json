{
  "name": "@derby-streams/scraper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun --env-file=../../.env run src/index.ts",
    "notify": "bun --env-file=../../.env run src/notify.ts",
    "test": "bun test",
    "typecheck": "bunx --bun tsc --noEmit"
  },
  "dependencies": {
    "@derby-streams/shared": "workspace:*",
    "cheerio": "^1.2.0",
    "playwright": "^1.62.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
