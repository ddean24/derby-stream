{
  "name": "derby-streams",
  "version": "0.1.0",
  "private": true,
  "description": "derby streams project",
  "type": "module",
  "workspaces": [
    "apps/*",
    "shared"
  ],
  "scripts": {
    "build": "bun run --filter '@derby-streams/web' build",
    "typecheck": "bun run --filter '@derby-streams/shared' --filter '@derby-streams/scraper' --filter '@derby-streams/web' typecheck",
    "lint": "echo 'lint script placeholder — no linter configured'"
  },
  "license": "UNLICENSED"
}
